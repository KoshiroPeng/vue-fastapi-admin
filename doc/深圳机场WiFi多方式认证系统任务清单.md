# 深圳机场 WiFi 多方式认证系统任务清单

> 本文件是项目执行状态的唯一任务清单。每次开始、完成或阻塞任务时必须同步更新状态、验证证据和执行记录。
>
> 最后更新：2026-09-10；当前任务：M2-03（只读运行配置摘要页面）。

## 状态说明

- `[ ]`：未开始
- `[-]`：进行中
- `[x]`：已完成并验证
- `[!]`：阻塞，必须在“阻塞项”中写明原因和解除条件

## 当前执行策略

1. 优先实现 FastAPI 认证前置网关；
2. 第二阶段实现运维管理台；
3. 当前无真实 NCE，所有开发和自动化测试使用 NCE Mock；
4. 外部服务通过统一客户端接口隔离，后续接入真实 NCE 时不得修改上层业务契约；
5. 旅客端 Portal H5 延后，当前只实现其所需后端领域能力和稳定 API 契约。

## 总体进度

| 阶段 | 状态 | 完成数 | 总数 | 说明 |
| --- | --- | ---: | ---: | --- |
| M1 认证网关与 NCE Mock | 已完成 | 9 | 9 | 网关、Mock、Redis、风控、取号机及安全基线已验证 |
| M2 运维管理台后端与前端 | 进行中 | 2 | 7 | 数据代理公共层和健康度页面已完成 |
| M3 第三方认证适配 | 未开始 | 0 | 5 | 先完成后端，不开发 Portal H5 |
| M4 旅客 Portal H5 | 延后 | 0 | 5 | 待前三阶段稳定后启动 |
| M5 联调、压测与上线 | 未开始 | 0 | 5 | 真实外部环境就绪后启动 |

---

## M1：FastAPI 认证前置网关与 NCE Mock

### M1-01 建立可持续任务清单

- [x] 建立本文件
- [x] 定义阶段、状态、依赖、验收标准和执行记录格式
- **验收证据**：任务清单已保存到项目 `doc` 目录，并已从根目录 README 链接

### M1-02 建立后端测试和依赖基线

- [x] 修复 Windows 下无条件安装 `uvloop` 的依赖问题
- [x] 引入 `pytest`、`pytest-asyncio`
- [x] 引入异步 Redis 客户端
- [x] 建立测试目录、测试配置和首个自动化测试
- [x] 将测试命令接入 Makefile
- **依赖**：无
- **完成标准**：Windows 环境依赖可安装，测试命令可运行且至少包含一个真实行为测试
- **验收证据**：`uv sync` 依赖解析成功；测试覆盖 NCE Mock、健康接口和配置脱敏

### M1-03 WiFi 配置模型与 Mock 开关

- [x] 增加 Redis、NCE、取号机、限流和超时配置
- [x] 默认启用 `NCE_MOCK_ENABLED=true`
- [x] 移除现有应用 `SECRET_KEY` 等敏感配置的生产默认值
- [x] 提供 `.env.example`，不包含真实密钥
- [x] 实现只读、脱敏的运行配置摘要
- **依赖**：M1-02
- **完成标准**：Mock 模式无需真实 NCE 即可启动；配置摘要不返回密码或 Secret
- **验收证据**：生产环境拒绝开发密钥和通配符 CORS；初始管理员默认关闭且启用时必须注入密码；配置摘要测试确认不返回任何 Secret

### M1-04 NCE 统一客户端与 Mock 实现

- [x] 定义 Token、访客、在线用户、RADIUS 日志和健康状态模型
- [x] 定义稳定的 NCE 客户端接口
- [x] 实现健康探测、Token、访客、用户查询和 RADIUS 日志的确定性 NCE Mock
- [x] Mock 支持成功、鉴权失败、业务失败、超时和不可用场景
- [x] 为未来真实 HTTP NCE 客户端保留实现边界
- **依赖**：M1-03
- **完成标准**：上层服务只依赖统一接口；Mock 场景有自动化测试
- **验收证据**：Token 使用 `SecretStr` 防止 repr 泄漏；用户查询支持过滤与分页；RADIUS 查询限制 7 天并使用不透明游标；五种 Mock 场景均有类型化异常和安全 HTTP 映射测试

### M1-05 Redis 生命周期与短时认证事务

- [x] 建立 Redis 连接和关闭生命周期
- [x] 支持开发测试内存替身，接口与 Redis 实现一致
- [x] 实现 `authTxId` 状态机与 TTL
- [x] 实现一次性消费
- [x] Redis 中禁止保存明文 MAC、密码、证件信息
- **依赖**：M1-02、M1-03
- **完成标准**：状态流转、过期和重复消费测试通过
- **验收证据**：已连接 WSL `ruoyi-redis`（Redis 7.2.14，`127.0.0.1:16379`）；真实 Redis 生命周期、TTL、状态流转、一次性消费和载荷安全集成测试通过

### M1-06 风控、幂等和防重放

- [x] 实现 MAC/IP 哈希与脱敏
- [x] 实现滑动窗口限流
- [x] 实现 Timestamp + Nonce 防重放
- [x] 实现 `Idempotency-Key` 去重
- [x] 返回 409、429 等明确 HTTP 状态码
- **依赖**：M1-05
- **完成标准**：篡改、过期、重复和超限请求测试通过
- **验收证据**：HMAC-SHA256 标识摘要、Redis 原子滑动窗口、Nonce/幂等摘要键和统一风险错误处理均有自动化测试；真实 Redis 集成验证覆盖超限、窗口恢复和重复提交

### M1-07 取号机创建访客端到端切片

- [x] 固化 HMAC-SHA256 签名规范
- [x] 实现 IP 白名单和签名鉴权
- [x] 实现 `POST /api/v1/kiosk/create-guest`
- [x] 通过 NCE Mock 创建 24 小时、单终端访客
- [x] 响应和日志禁止泄漏内部 Token
- **依赖**：M1-04、M1-06
- **完成标准**：合法请求成功；篡改、重放、重复请求和非法来源被拒绝
- **验收证据**：真实 Redis 集成测试覆盖合法创建、篡改签名、Nonce 重放、幂等冲突、非法来源 IP 和 Redis 载荷检查；接口契约已同步回详细设计说明书

### M1-08 网关健康度与可观测性

- [x] 实现 traceId/requestId
- [x] 实现结构化脱敏日志
- [x] 实现 NCE Mock 健康探测
- [x] 实现 `GET /api/v1/dashboard/health`
- [x] 模拟 NCE 不可用并验证降级状态
- **依赖**：M1-04
- **完成标准**：健康接口能展示 `healthy/degraded/down`，失败原因可排查且无敏感数据
- **验收证据**：请求 ID 支持安全透传或自动生成并写入响应头；NCE/Redis/风控/取号机日志包含可查询事件和 requestId；健康接口同时返回 NCE 与 Redis 状态；真实 WSL Redis 健康测试通过

### M1-09 现有应用安全基线加固

- [x] 删除或严格限制 `token == "dev"` 鉴权旁路
- [x] 移除默认管理员弱口令
- [x] 生产环境强制注入 `SECRET_KEY`
- [x] 收紧 CORS 来源与凭据策略
- [x] Portal、取号机和第三方回写请求禁止原文写入审计表
- **依赖**：M1-02
- **完成标准**：安全负例测试通过，日志和审计表中不存在认证凭据或旅客 PII
- **验收证据**：无效 `dev` Token 返回 401；管理员初始化改为显式配置；密码重置生成至少 16 位随机密码；审计递归脱敏；校验错误不回显原始输入；Portal/取号机接口排除原文审计

---

## M2：运维管理台

### M2-01 管理台数据代理公共层

- [x] 管理台 JWT 和现有路径权限复用
- [x] 统一分页、时间范围、脱敏和 NCE 异常映射
- [x] 增加短时查询缓存与并发请求合并
- **依赖**：M1-04、M1-08
- **验收证据**：管理接口继续使用现有 `DependPermission`；在线用户采用页码分页，RADIUS 采用最多 7 天的不透明游标；所有账号/IP/MAC 在代理层脱敏；NCE 异常使用稳定 502/504 映射；Redis 短时缓存通过并发五请求只加载一次的真实集成测试

### M2-02 外部服务健康度页面

- [x] 健康度 API 权限
- [x] 外部服务状态页面
- [x] 正常、波动、不可用状态展示
- **依赖**：M1-08、M2-01
- **验收证据**：健康 API 继续受现有路径权限保护；页面展示 NCE 与 Redis 的健康、波动、离线、延时、配置和 Mock 模式，并提供手动刷新、错误态及移动端布局；菜单入口将在 M2-07 统一初始化

### M2-03 只读运行配置摘要页面

- [ ] 配置摘要 API 权限
- [ ] 只显示是否配置、开关、超时和非敏感 ID
- [ ] 禁止在线修改
- **依赖**：M1-03、M2-01

### M2-04 RADIUS 准入日志

- [x] NCE Mock 游标分页
- [x] 时间范围与过滤条件
- [x] 手机号、用户名、MAC、IP 脱敏
- [ ] 准入日志页面
- **依赖**：M1-04、M2-01

### M2-05 在线用户

- [x] 在线用户代理 API
- [ ] 在线用户页面
- [ ] 踢线功能默认关闭
- **依赖**：M1-04、M2-01

### M2-06 认证统计看板

- [ ] 明确 Mock 统计口径
- [ ] 指标卡、趋势、认证方式和失败原因 API
- [ ] 看板页面和图表组件
- **依赖**：M2-04、M2-05

### M2-07 菜单、角色和 API 权限初始化

- [ ] 新增 WiFi 运营、日志和运维菜单
- [ ] 初始化过程可重复执行
- [ ] 验证查看、日志、配置和踢线权限隔离
- **依赖**：M2-02～M2-06

---

## M3：第三方认证后端适配

- [ ] M3-01 登机牌验证客户端、Mock、风控和认证编排
- [ ] M3-02 护照 OCR 客户端、Mock、MRZ 校验和真正零落盘上传
- [ ] M3-03 微信状态回写、验签、Nonce、幂等和状态查询
- [ ] M3-04 第三方调用摘要表、异步写入、采样和保留期清理
- [ ] M3-05 登机牌、护照、微信后端契约测试与 OpenAPI 基线

## M4：旅客 Portal H5（延后）

- [ ] M4-01 Portal 独立布局、入口参数解析和认证方式选择
- [ ] M4-02 短信认证页面及 NCE 原生 DOM/JS 兼容
- [ ] M4-03 护照拍照上传和认证状态页面
- [ ] M4-04 登机牌三要素和微信小程序页面
- [ ] M4-05 iOS CNA、Android Captive Portal、微信浏览器兼容性验证

## M5：联调、压测与上线

- [ ] M5-01 接入真实 NCE 并完成契约测试
- [ ] M5-02 接入真实登机牌、OCR、微信和取号机系统
- [ ] M5-03 安全测试、PII 泄漏扫描和故障注入
- [ ] M5-04 并发压测、双实例部署和回滚演练
- [ ] M5-05 灰度割接、全量上线和观察期复盘

---

## 当前开发环境

| 组件 | 配置 | 状态 | 说明 |
| --- | --- | --- | --- |
| NCE | Mock | 可用 | 默认场景 `healthy`，可切换 `unavailable` |
| Redis | `127.0.0.1:16379` | 可用 | WSL 容器 `ruoyi-redis`，Redis 7.2.14，无密码；本地地址保存在被 Git 忽略的 `.env` |

## 当前阻塞项

| 编号 | 阻塞项 | 影响 | 当前处理方式 | 解除条件 |
| --- | --- | --- | --- | --- |
| B-001 | 暂无真实 NCE 环境 | 无法验证真实 Token、访客创建、在线用户和 RADIUS 契约 | 使用统一 NCE Client 接口和确定性 Mock 开发 | 获得测试地址、账号、站点和访客用户组后执行真实契约测试 |
| B-002 | 取号机 HMAC 规范未与设备方确认 | 后续可能需要调整签名字段 | 已提供固定 Canonical String、测试密钥和预期签名；已处理可信代理来源 IP | 设备方确认请求头、Canonical String、测试向量和时钟偏差 |

## 执行记录

| 时间 | 任务 | 动作 | 结果/证据 | 下一步 |
| --- | --- | --- | --- | --- |
| 2026-09-10 | 计划重排 | 确认先网关、后管理台、Portal H5 延后 | 用户确认，当前 NCE 使用 Mock | 建立任务清单并启动 M1 |
| 2026-09-10 | M1-01 | 建立持久化任务清单 | 本文件创建 | 执行 M1-02 |
| 2026-09-10 | M1-02 | 检查测试及 Redis 依赖 | 当前环境缺少 `pytest` 和 `redis`；发现 Windows 下重复的无条件 `uvloop` 依赖 | 修复依赖并建立首个行为测试 |
| 2026-09-10 | M1-02 | 修复依赖并建立测试基线 | 移除 Windows 无条件 `uvloop`，引入 Redis、pytest、pytest-asyncio；Makefile 使用 `uv run python -m pytest` | 完成 M1-02 |
| 2026-09-10 | M1-03 | 增加 WiFi 配置和脱敏摘要 | 默认启用 NCE Mock，新增 `.env.example` 和 `/runtime-config/summary` 后端接口 | 继续处理现有应用 Secret 安全基线 |
| 2026-09-10 | M1-04 | 建立 NCE 客户端边界 | NCE Mock 已支持健康探测、访客创建和不可用场景 | 补充 Token、在线用户、RADIUS 和错误场景 |
| 2026-09-10 | M1-08 | 建立首个管理端健康接口 | `/api/v1/dashboard/health` 已接入现有路径权限；测试覆盖 Mock 健康响应 | 后续增加 traceId 和结构化日志 |
| 2026-09-10 | 文档接入 | 从根目录 README 链接详细设计、实施计划和任务清单 | 后续人员可直接查看范围、顺序与实时进度 | 每次任务状态变化同步更新本文件 |
| 2026-09-10 | M1-05 | 接入 WSL Redis | `ruoyi-redis` 连接成功，版本 7.2.14；本地 `.env` 使用 `redis://127.0.0.1:16379/0` | 实现 Redis 认证事务存储 |
| 2026-09-10 | M1-05 | 完成短时认证事务 | 内存与 Redis 存储支持 TTL、合法状态流转、一次性消费和原子更新；真实 Redis 集成测试通过 | 启动 M1-06 风控、幂等和防重放 |
| 2026-09-10 | M1-06 | 完成标识脱敏和摘要 | MAC/IP 规范化后使用带命名空间的 HMAC-SHA256 摘要；新增独立 `PII_HASH_SECRET` | 取号机只能把摘要传入事务和限流模块 |
| 2026-09-10 | M1-06 | 完成 Redis 风控原语 | 滑动窗口限流、Timestamp 校验、Nonce 防重放和幂等占位全部完成；Redis 键不保存原始 Nonce/幂等键 | 启动 M1-07，严格按“验签→限流→防重放→幂等→NCE”顺序编排 |
| 2026-09-10 | M1-06 | 完成风险错误响应 | 频控返回 429 与 `Retry-After`；重放和幂等冲突返回 409；响应不暴露内部输入 | M1-07 复用统一异常处理 |
| 2026-09-10 | M1-07 | 固化取号机 HMAC 契约 | 签名绑定方法、路径、取号机 ID、时间戳、Nonce、幂等键和原始请求体摘要；详细设计同步更新 | 等待设备方最终确认测试向量 |
| 2026-09-10 | M1-07 | 完成取号机 Mock 端到端链路 | IP 白名单、验签、时钟偏差、限流、防重放、幂等和 NCE Mock 创建访客已串联；敏感接口排除请求/响应原文审计 | 启动 M1-09 安全基线加固 |
| 2026-09-10 | M1-07 | 加固代理来源 IP 与联调契约 | 仅信任配置内代理的 `X-Forwarded-For`；增加固定 HMAC 测试向量，覆盖直接伪造和可信代理转发场景 | 设备方按测试向量确认实现 |
| 2026-09-10 | 安全复核 | 两次只读审查子代理均在 180 秒超时且未修改文件；经用户确认改用本地审查 | 发现并修复代理来源 IP 信任、校验错误回显原始输入、请求体无上限三个问题 | 全量执行安全负例与真实 Redis 测试 |
| 2026-09-10 | M1-07 | 限制取号机请求体 | 应用层限制 8KB 且拒绝额外 JSON 字段；Nginx 对精确接口同步设置 `client_max_body_size 8k` | 防止白名单来源发送大请求消耗内存 |
| 2026-09-10 | M1-09 | 移除认证旁路与弱口令 | 删除 `dev` Token；初始管理员改为显式开关和注入密码；密码重置改为一次性随机强密码 | 前端只展示一次临时密码，审计存储时自动脱敏 |
| 2026-09-10 | M1-09 | 强化生产配置门禁 | 新增 `APP_ENV`；生产环境拒绝开发 `SECRET_KEY`、短密钥和通配符 CORS | 部署必须显式提供生产 Secret 和来源列表 |
| 2026-09-10 | M1-09 | 完成审计与错误脱敏 | 审计递归过滤密码、Token、Secret、手机号和证件摘要；参数错误不再回显原始输入；Portal/取号机排除原文审计 | 启动 M1-04 NCE Mock 能力补全 |
| 2026-09-10 | M1-04 | 补全 NCE 领域契约 | 新增 Secret Token、用户分页、在线状态、RADIUS 查询/游标及日志模型；查询窗口固定不超过 7 天 | 管理台后端可直接复用稳定接口 |
| 2026-09-10 | M1-04 | 补全 NCE Mock 故障场景 | 支持 `healthy`、`auth_failure`、`business_failure`、`timeout`、`unavailable`；统一映射为安全的 502/504 响应 | 启动 M1-08 可观测性收尾 |
| 2026-09-10 | M1-08 | 完成请求链路标识 | 合法 `X-Request-ID` 安全透传，非法值替换为随机 ID；响应返回同一 ID；关键决策日志关联 requestId | 管理台代理接口复用该链路标识 |
| 2026-09-10 | M1-08 | 完成依赖健康聚合 | `/api/v1/dashboard/health` 同时返回 NCE Mock 与 Redis 状态；Redis 探测不暴露连接地址和异常原文 | M1 全部任务完成，进入 M2-01 |
| 2026-09-10 | M2-01 | 完成管理台数据代理公共层 | 新增只保存脱敏结果的 Redis 查询缓存与进程内 single-flight；在线用户和 RADIUS 代理统一分页、时间窗、脱敏及 NCE 错误语义 | 启动 M2-02 外部服务健康度页面 |
| 2026-09-10 | M2-04/M2-05 | 提前完成管理端数据 API | 新增 `/dashboard/online-users` 与 `/dashboard/radius-logs`，Mock 数据经过代理层脱敏后返回 | 后续实现对应 Vue 页面和菜单权限 |
| 2026-09-10 | M2-02 | 完成外部服务健康度页面 | 新增机场运行监控风格页面，展示 NCE/Redis 状态、延时、配置、Mock 提示、刷新与错误反馈；前端 API 已接入 | 启动 M2-03 只读运行配置摘要页面 |
